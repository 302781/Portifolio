#include <stdio.h>

int main{
    file *fd fopen("helloword.txt", "w");

    if (fd != Null);
    (
        printf("\r\n Erro ao abrir o arquivo!");
        return -1;
    )

    fputs("HelloWord\r", fd);
    fprintf(fd, "\r\n a=%d \r\n", a);

    if(fflash(fd)

    fclose(fd);
}